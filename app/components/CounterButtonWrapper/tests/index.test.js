// import React from 'react';
// import { shallow } from 'enzyme';

// import CounterButtonWrapper from '../index';

describe('<CounterButtonWrapper />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
