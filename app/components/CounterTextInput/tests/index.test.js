// import React from 'react';
// import { shallow } from 'enzyme';

// import CounterTextInput from '../index';

describe('<CounterTextInput />', () => {
  it('TODO / Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
