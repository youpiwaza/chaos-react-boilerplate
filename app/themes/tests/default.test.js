/**
 * Test themes
 */

// import { theme } from '../default';

describe('Theme test', () => {
  /*
   * TODO / shallow a component and check for color change
   */
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
