// import React from 'react';
// import { shallow } from 'enzyme';

// import { Counter } from '../index';

describe('<Counter />', () => {
  it('TODO / Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });

  // TODO
  // it should render the header, textinput & both button
  // it should update textinput value when props.count change
  // it should dispatch(incrementAction()) when + button is clicked
  // it should dispatch(decrementAction()) when - button is clicked
  // it should dispatch(setAction(value)) when textInput value is changed by user
});
