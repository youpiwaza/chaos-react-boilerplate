// import { fromJS } from 'immutable';
// import { selectErrorBoundaryDomain } from '../selectors';

describe('selectErrorBoundaryDomain', () => {
  it('TODO / Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
